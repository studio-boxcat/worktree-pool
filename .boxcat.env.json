{
  "$schema": "../boxcat-devenv/boxcat.env.schema.json",
  "env": [
    {
      "name": "WORKTREE_ROOT",
      "value": "$HOME/.worktree-pool",
      "why": "where the pool keeps its worktrees"
    }
  ],
  "typecheck": {
    "run": "just typecheck",
    "why": "cargo is this repo's compiler, and `cargo check` its no-emit mode; the flags live in the justfile with its other cargo lines."
  },
  "bin": [
    {
      "name": "wt",
      "path": "bin/wt",
      "usage": "the pool workflow every build recipe drives"
    }
  ],
  "hooks": {
    "setup": {
      "run": "just install",
      "why": "cargo install of the binary — a build, so a script."
    },
    "doctor": {
      "run": "worktree-pool doctor",
      "why": "Arch, git, quarantine, pools — what only the binary can assert."
    }
  }
}
